import styled from "styled-components"

export const StyleForm = styled.div`
display: grid;
gap: 8px;
margin-bottom: 24px;

.btn{
    background-color: green;
    border: none;
    border-radius: 4px;
    height: 24px;
}
` 