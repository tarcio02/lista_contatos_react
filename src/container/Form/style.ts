import styled from "styled-components"

export const StyleForm = styled.form`
display: grid;
gap: 8px;
margin-bottom: 24px;

input{
    width: 100%;
}

.btn{
    background-color: green;
    border: none;
    border-radius: 4px;
    height: 24px;
}
` 